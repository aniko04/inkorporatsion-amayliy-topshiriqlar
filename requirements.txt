# «Inkorporatsion amaliy topshiriqlar» — kerakli paketlar (bitta fayl).
# O'rnatish:  python -m pip install -r requirements.txt
#
# Versiyalar aynan sinovdan o'tgan holatga qadalgan. Django 5.2 — LTS
# (uzoq muddat qo'llab-quvvatlanadigan) chiqarilishi.

Django==5.2.14
whitenoise==6.12.0

# Django'ning o'z bog'liqliklari (aniqlik uchun ochiq yozildi)
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2026.2

# Serverda saytni ishga tushiradigan WSGI server. `manage.py runserver` faqat
# ishlab chiqish uchun: bitta oqim, yuk ostida sinalmagan. Waitress toza
# Python'da yozilgan, Windows'da ham Linux'da ham bir xil ishlaydi:
#     python -m waitress --listen=127.0.0.1:8000 --threads=8 core.wsgi:application
# Batafsil: DEPLOY.md
waitress==3.0.2
