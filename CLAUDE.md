# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

A Django 5.2 (LTS) web app: an Uzbek-language **methodological e-platform** ("Inkorporatsion amaliy topshiriqlar") built around a dissertation on teaching primary-school technology (handicraft) via an *inkorporatsion* approach. UI text, code comments, and model `verbose_name`s are in Uzbek — match that language when writing user-facing strings.

Two audiences share one site: **teachers/researchers** (the outer shell — seven methodological sections) and **children** (the 10 interactive exercises, which now live inside the *Multimediya* section).

## Commands

The project runs against the checked-in virtualenv at `env/` (Windows). Invoke Python directly through it:

```bash
env/Scripts/python.exe manage.py runserver      # dev server
env/Scripts/python.exe manage.py check          # validate config (run after editing views/urls)
env/Scripts/python.exe manage.py makemigrations
env/Scripts/python.exe manage.py migrate
env/Scripts/python.exe manage.py collectstatic  # into staticfiles/ (WhiteNoise compresses)
env/Scripts/python.exe manage.py createsuperuser
env/Scripts/python.exe manage.py zaxira         # baza zaxirasi (zaxira/ ga)
```

There is no automated test suite — `home/tests.py` is empty boilerplate. Verify changes by running the server and exercising the affected flow in the browser.

Note: `DEBUG = False` even in development (`core/settings.py`). WhiteNoise serves static files, so `runserver` works without extra config.

**Two gotchas that will waste your time if you forget them:**
- Because `DEBUG = False`, Django uses the **cached template loader**. Editing a `.html` file has *no effect until you restart the server* — the autoreloader does not clear the template cache. CSS/JS edits do take effect (after `collectstatic`), which makes the staleness easy to misdiagnose.
- WhiteNoise indexes `staticfiles/` **once, at startup**. A file that appears there afterwards (a new stylesheet, a new image folder, a throwaway page you dropped in) **404s until the server restarts** — so a brand-new page can render as unstyled text with broken images while the old assets keep working. If a page suddenly looks like raw HTML, suspect this before touching the CSS. A missing stylesheet also lets inline `<svg>` icons render at their intrinsic size, i.e. enormous — give icons explicit `width`/`height` attributes so that failure mode stays cosmetic.
- Editing templates from PowerShell with `Set-Content -Encoding UTF8` writes a **UTF-8 BOM** (PS 5.1 behaviour), which then leaks in front of `<!DOCTYPE html>`. Use the `Edit`/`Write` tools, or `[System.IO.File]::WriteAllText` with `UTF8Encoding($false)`.

## Architecture

**Apps:** `core` (settings/urls/wsgi) and `home` (everything real). A third app, `temp`, was empty boilerplate and was **deleted 2026-09-09** along with its `INSTALLED_APPS` entry — don't recreate it.

### The platform shell (seven sections)

`templates/base.html` is the shell for every non-exercise page: sticky navbar, the seven sections, footer, and an inline SVG sprite holding the two signature motifs (`#palak` — a suzani rosette; `#orama` — a quilling spiral). Its stylesheet is `static/css/platform.css`, a self-contained design system that deliberately **does not** load `style.css`.

| # | Section | URL | Template | View |
|---|---------|-----|----------|------|
| 1 | Muallif haqida | `/haqida` | `haqida.html` | `haqida` |
| 2 | Metodika | `/metodika` | `bolim.html` | `metodika` |
| 3 | Maqola | `/maqola` | `bolim.html` | `maqola` |
| 4 | Multimediya | `/multimediya` | `multimediya.html` | `multimediya` |
| 5 | Topshiriq | `/topshiriq` | `bolim.html` | `topshiriq` |
| 6 | Instruksion texnologik xarita | `/xarita` | `bolim.html` | `xarita` |
| 6a | └ Origami … «Organayzer» … | `/xarita/qalamdon` | `qalamdon.html` | `qalamdon_sahifa` |
| 6b | └ Applikatsiya … «Xonqizi» … | `/xarita/xonqizi` | `xonqizi.html` | `xonqizi_sahifa` |
| 7 | Dars ishlanmalar | `/ishlanma` | `bolim.html` | `ishlanma` |
| — | O'quvchilarim *(faqat admin)* | `/oquvchilarim` | `oquvchilarim.html` | `oquvchilarim` |
| — | └ Bitta kishining natijalari | `/oquvchilarim/<id>` | `oquvchi_natija.html` | `oquvchi_natija` |

- The landing page (`/`, `templates/index.html`) is the platform pitch: hero, the seven-section grid, the "Platforma haqida" article. It is **not** the exercise list any more.
  - **The hero is built to the client's mockup (2026-09-04)** — an illustrated scene (`static/img/bosh-fon.jpg`, 1855×848: boy left, girl right, desk below, **centre deliberately empty**) with everything else real HTML/CSS in the `.bosh` block of `platform.css` ("QAHRAMON BLOKI"): rainbow «Inkorporatsion» (an SVG `<text>` in three layers — white ring, per-letter gradient `<tspan>`s, clipped gloss; the `rotate`/`dy` lists give the hand-set bounce), blue pill «amaliy topshiriqlar», yellow ribbon, a cloud (union of circles + a loose dashed outline in one SVG), five section circles, green CTA, and the white wave band with four taglines. Two mechanics matter:
    - **Hero height comes from the image's aspect ratio** (`aspect-ratio: 1855/848`, `background-size: cover`), capped at `100vh − 150px` (navbar 75 + band 64). So the picture is never cropped and there is no blurred filler band; on short windows only a few px go top/bottom. `contain` + a blurred copy was tried first and left a pale strip above the sky.
    - **Everything inside is sized in `cqw` of `.bosh-ichi`**, whose width is `min(45% of hero, 98% of (100vh−150px), 920px)` — i.e. the gap between the children, or the height, whichever binds. Widen it and the text lands on the children's faces. `max(Xrem, Ycqw)` floors keep it readable on mobile, where the cloud SVG is hidden and the content sits on a plain white plate (`≤900px`, `bosh-fon-mob.jpg`). The cloud SVG's viewBox (1120×610) matches the box's measured aspect (≈2.22) so `preserveAspectRatio="none"` doesn't distort it — recompute if you change the cloud's padding or font sizes.
    - Testing narrow layouts in headless Chrome: `--window-size=390,…` silently lays out at ~500px (Chrome's minimum window width) and crops the screenshot, which looks like a horizontal-overflow bug. And the site can't be iframed from a `file://` harness because of `X-Frame-Options: DENY`. What works: `curl` the page, rewrite `/static/` to absolute `http://127.0.0.1:PORT/static/` URLs, save it locally, and iframe *that* file at the width you want (an inline `<script>` in the copy can also print `getBoundingClientRect()` numbers for `--dump-dom`).
- **Inner pages follow the same bright language (2026-09-05)** — the block "YORQIN USLUB — ICHKI SAHIFALAR" at the end of `platform.css`. Every `.page-head` is a sky banner: gradient + two SVG clouds (one data-URI image kept in `--bulut-rasm`, reused by `auth.css`) with a **wavy bottom edge** cut by `mask-image` on `.pf .page-head::before`, and a big section-coloured circle icon on the right from the `templates/_sahifa_ikon.html` partial (included as the first child of each `.page-head .wrap`; the glyph is chosen by `active`). Cards are 26–28px rounded, every button is a pill with a hard bottom shadow, and the landing's seven cards carry the same circle icons as the hero (`.bolim-ikon`). Three cascade facts that bit while building it:
  - The four interactive pages' stylesheets (`qalamdon.css`, `xonqizi.css`, `rasmli-test.css`, `diktant.css`) load **after** `platform.css` and still set the old photo on `.ql-head::before` etc. at (0,1,1). That is why the sky rule is written as `.pf .page-head::before` — drop the `.pf` and the photos come back.
  - An earlier block (BO'LIM TUSLARI) had turned the same pseudo-element into a 200px faint circle; a later rule that only sets `inset: 0` inherits `width/height: 200px; opacity: .05` and renders as nothing. Reset those explicitly when repurposing a pseudo-element.
  - `<symbol id="palak">` has no `class="palak"`, so the original `.palak .petal { fill: currentColor }` never matched and every palak rendered **black** since the first redesign. `#palak .petal` fixes it site-wide; palak instances now take their `color`.
  - `.page-head::before` occurs three times in the file (photo era, bright era `content: none`, inner-pages sky). A script that searches for the first match edits the dead one — anchor on the last occurrence.
- **Sections 2, 3, 5, 6, 7 are admin-driven.** They all render `bolim.html` through the `_bolim()` helper in `home/views.py`; content comes from the `Material` model (`section`, `title`, `summary`, `body`, `file`, `muqova`, `link`, `manba`, `order`, `is_published`). To add a section, add a choice to `Material.SECTION_CHOICES` and a thin view calling `_bolim()`. Empty sections render a designed empty state, with an "add material" link for staff. Two of them tint their cards in alternating pairs, keyed off the `bolim-{{ active }}` class: **Metodika** `--limon`/`--yashil`, **Maqola** `--gulnor`/`--nilufar` (rose/periwinkle). Both pairs live in `platform.css`, so editing them means bumping `platform.css?v=` in **all three** of `base.html`, `login.html`, `register.html`. `.material-tag` and `.material-muqova` are forced back to `--paper` inside a tinted section, or the cover and badge dissolve into the card. **Add materials as `Material` rows, never as hand-written HTML in `bolim.html`** — the admin-managed flow is the client's explicit choice.
  - `muqova` is an optional cover image (`FileField`, **not** `ImageField` — Pillow is not installed in `env/`, and `ImageField` would fail `manage.py check`). Filled in → the card shows a book cover; empty → the old file-extension badge. Covers for PDFs are rendered from page 1 with PyMuPDF at 460px wide, JPEG q82 (~20 KB), into `media/materiallar/muqova/`. All 13 Metodika and all 16 Maqola rows have one. Page 1 is not always the cover, though: for seven Maqola articles the PDF starts on plain text because the journal's cover is a separate file, and the client supplied those seven images by hand — check what a render actually produced before assuming the whole section can be regenerated from the PDFs.
  - Materials with a viewable PDF (`Material.koruv_url`) get a **"Ko'rish"** button that reveals an inline `<iframe>`; its `src` is set on first click so a 10 MB book is not fetched on page load. One delegated click handler in `bolim.html` covers every card. `koruv_url` returns `file` when it is a PDF, otherwise the optional **`koruv`** field — a PDF rendition of a Word file, since browsers can't frame `.doc`/`.docx`. "Dars ishlanmalar" is all Word, so each row has a `koruv` PDF in `media/ishlanmalar/koruv/` while "Yuklab olish" still hands over the editable original. Word itself does the conversion (`Word.Application` COM → `ExportAsFixedFormat($out, 17)`); LibreOffice is not installed.
  - **Sub-sections (`Material.kichik_bolim`)** — a section can split its materials into collapsible `<details>` groups. Only **Topshiriq** uses this so far: *Rasmli test* and *Texnologik diktantlar*, from `Material.KICHIK_BOLIM_CHOICES`, wired up by passing `guruhlash=` to `_bolim()`. The groups always render, even while empty, so the page doesn't change shape as the client fills them; a material with a blank `kichik_bolim` falls back to a flat card above the groups. The card markup itself lives in the `templates/_material.html` partial, included from both the flat list and each group — edit it there, not in `bolim.html`.
    - **«Rasmli test» is a real interactive test page**, not just a file list: `/topshiriq/rasmli-test` → `rasmli_test_sahifa` → `templates/rasmli_test.html` + `static/css/rasmli-test.css`. Questions and the answer key live in **`home/rasmli_test.py`** (15 tasks, 33 answer rows × 5 points = 165, 20-minute timer) — built from `malumotlar/topshiriqlar/Rasmli test.docx`, whose 45 pictures were extracted to `static/img/rasmli-test/sNN{a,b,s}.jpg` (task 13's multi-tool options are composited into one tile each). Four rules decide how a picture looks, each learned the hard way: (1) alpha must be flattened onto white or transparent PNGs render **black**; (2) **quality is per-content** — Word's own JPEGs are copied byte-for-byte, PNG photos go q92, and clipart/line art goes **q97 with `subsampling=0`**, because plain q82 visibly mangles flat colour; (3) **never upscale** — every source is 180–320px, so `.rt-variant-rasm` is a fixed-height white box and the `<img>` sits at its natural size inside it (`width:auto; max-width/height:100%`), which is why it looks sharp; (4) **every file is `.jpg`, deliberately** — an earlier pass wrote clipart as `.png`, so regenerating the images silently changed five URLs and the running server served 404s for them. The filename must depend only on question number and letter. `home/apps.py` registers a system check (`home.W001`) that fails `manage.py check` when any of the 45 files is missing, so a broken extraction can't reach the page unnoticed. Image URLs carry `?v=N` from `RASM_VERSIYA` — bump it whenever a picture is replaced under an existing name, or browsers keep showing the old one. The tiles also deliberately **do not** use `loading="lazy"`: the whole set is ~1 MB, and a tile that hasn't loaded yet is an unanswerable question. Two question shapes: single-answer (click a picture) and matching (3 pictures + N labelled rows, one A/B/S each). **The Word contains no answer key** — it was derived by looking at the pictures, so if the client disputes an answer, fix the letter in `_XOM` and nothing else. Scoring/timer are inline JS in the template (project convention); a logged-in user's result is POSTed to `/api/save-result` under the `rtest` key (added to `Result.MASHQ_CHOICES`). **The test opens behind a start gate** (`.rt-parda` + `.rt-boshla`): the page renders blurred under a modal, and only the "Boshlash" click hides it and calls `soatniBoshla()` — the timer no longer starts on the first answer, so a child who leaves the page open doesn't lose time. Question cards **alternate sky-blue (`--osmon` #E1EEFA) and violet (`--binafsha` #EDE6F9)** via `.rt-savol:nth-child(odd/even)` — the same idea as Metodika's `--limon`/`--yashil` pair, but these two tokens live in `rasmli-test.css` since only this page uses them. Two constraints when touching these: the inner `.rt-variant-rasm` **must stay pure white**, because the extracted images were flattened onto white and any tint draws a white rectangle around every picture; and the tiles/rows (`.rt-variant`, `.rt-qator`) use `rgba(255,255,255,0.62)` rather than `--stone`, so they read as a lighter shade of whichever card colour they sit on while still separating from that white image box. The correct/wrong states (`.rt-togri`/`.rt-xato`) are two-class selectors and so still win over the translucent default. The blur is `backdrop-filter` on the overlay, **never `filter` on the content** — a `filter` on an ancestor makes it the containing block for `position: fixed`/`sticky`, which would tear the navbar, the sticky panel and the floating clock out of place. Note the gate is unrecoverable if the inline JS dies (see the Google Fonts rule below); that is deliberate — failing visibly beats letting a pupil answer 33 questions into a dead "Tekshirish" button. **Leaving mid-test is also guarded**: while `soatId !== null && !tekshirildi`, a capture-phase click handler `confirm()`s before following any link (navbar "Chiqish" included) and `beforeunload` covers tab-close/reload. The two share a `chiqishgaRuxsat` flag so a confirmed exit isn't asked twice; both go quiet once `tekshir()` has run, because only then has the score been POSTed anywhere. A group can advertise such a page via `_bolim(..., ilova={kalit: {...}})`, which renders the indigo `.guruh-ilova` call-to-action above that group's materials.
    - **«Texnologik diktantlar» is a four-stage matching exercise**: `/topshiriq/diktantlar` → `diktant_sahifa` → `templates/diktant.html` + `static/css/diktant.css` (`.dk-` prefix, so it can never collide with the test's `.rt-`). Data lives in **`home/diktant.py`**, built from `malumotlar/topshiriqlar/Texnologik diktantlar (2).docx`: the Word's **four variants become four sequential stages**, 4 tasks each (16 total), 4 rows per task × 5 points = **80 per stage, 320 overall**, 5 minutes per stage. Each task shows 4 numbered pictures and 4 names; the pupil assigns a number to each name.
      - **Every number is used exactly once per task** — verified for all 16 tasks — so the UI enforces it: clicking a number that is already used elsewhere *moves* it rather than duplicating it, and the numbers still free elsewhere render at `opacity: 0.4` (`.band`). This kills half the possible mistakes before they happen.
      - **The answer key was in the Word itself** (the second column of each task's `name | number` table), unlike the Rasmli test where it had to be inferred. One place contradicts itself: variant 1 task 3 swaps "Gazeta"/"Yozuv qog'oz" (picture 1 is a notebook, picture 4 a newspaper) while variant 3 task 1 labels *those same two pictures* correctly. `_XOM` carries the corrected numbers with a comment; if the client disputes it, flip those two and nothing else. Two spellings were also fixed: "Robortlarning" → "Robotlarning", and `sanʼati` uses U+02BC like the rest of the file.
      - Images: 64 files at `static/img/diktant/v{variant}s{task}_{n}.jpg` (~1.7 MB). Same four rules as the Rasmli test — Word's own JPEGs copied byte-for-byte, PNGs flattened onto white then q92 (photo) or q97/`subsampling=0` (clipart), never upscaled (sources are 150–700px, so `.dk-rasm-quti` is a fixed-height white box), and always `.jpg`. Some pictures repeat across variants (paper types appear in all four) but each slot gets its own file, so a filename depends only on variant/task/slot. `home/apps.py` registers `home.W002` alongside `W001` to fail `manage.py check` when any of the 64 is missing.
      - **Every task is re-shuffled on each page load** (`diktant.aralashtirilgan()`), because the Word's own key was too regular to be a test: the paper-type tasks all keyed 4-2-3-1 and variant 1 task 1 keyed 1-2-3-4, so a pupil could score without looking at the pictures. Two things are permuted per task — which picture carries which number, and the order of the name rows — and the identity/reverse row orders are re-drawn (2 of 24) so a shuffle never *looks* unshuffled. `_XOM` and the `bosqichlar()` cache stay untouched; the shuffle builds fresh lists over them. **The view must pass its own shuffled list to `javob_kaliti(tuzilma)`** — calling it bare returns the unshuffled key and every answer scores wrong.
      - Task cards alternate **peach (`--shaftoli` #FCEADB) and mint (`--pudina` #E0F1EB)**, the third such pair on the site after Metodika's `--limon`/`--yashil` and the Rasmli test's `--osmon`/`--binafsha`; the tokens live in `diktant.css`. The selector is `:nth-of-type`, not `:nth-child` — a stage also contains a `<header>` and the result `<div>`, and only the tasks are `<article>`, so the count restarts cleanly at 1 in every stage. The same two constraints as the Rasmli test apply: `.dk-rasm-quti` **must stay pure white** (the images were flattened onto white), and `.dk-rasm`/`.dk-qator` use `rgba(255,255,255,0.62)` rather than `--stone`, which goes muddy over a tint. `.dk-ball` had to lose its translucent saffron fill for an opaque white one, or it read as two different badges on the two card colours.
      - Stage gating: finishing a stage (pressing "Bosqichni tekshirish") unlocks the next one **regardless of score** — a 2nd-grader shouldn't get stuck. Each stage's timer starts when that stage opens, and each stage POSTs its own `Result` row under `dikt1`…`dikt4` (added to `Result.MASHQ_CHOICES`, migration `0009`), so the teacher can see *which* stage went wrong. Stage switching is pure JS — one page, `hidden` toggled, no reload — and the start gate + exit guard work exactly as in the Rasmli test.
    - The **navbar "Topshiriq" item is a hover menu** listing the same two sub-sections (`base.html` → `.nav-item` / `.nav-menu`, CSS-only: `:hover` + `:focus-within`, no JS). Both entries now link straight to their interactive page.
      Anchor plumbing for the group links: each `<details>` carries `id="{{ g.kalit }}"`, and a small script in `bolim.html` opens the targeted group on load *and* on `hashchange` (needed because a `<details>` doesn't open from a hash on its own, and because clicking the menu while already on the page only changes the hash). `.guruh` has `scroll-margin-top: 94px` so the sticky navbar doesn't cover the summary. Below the 1365px hamburger breakpoint the panel goes `position: static` and the two entries simply sit indented under "Topshiriq" — there is no hover on touch.
  - **Instruksion texnologik xarita has two hand-built pages** (renamed 2026-09-05 to the client's own wording: «Origami usulidan foydalanib «Organayzer» yasash texnologiyasi» and «Applikatsiya usulidan foydalanib «Xonqizi» yasash texnologiyasi»; the code still calls them *qalamdon* and *xonqizi* everywhere) — `/xarita/qalamdon` → `qalamdon_sahifa` → `templates/qalamdon.html` + `static/css/qalamdon.css` (`.ql-` prefix), data in `home/qalamdon.py`, built from `malumotlar/texnologikxarita/qalamdon/`. The section page advertises them through **`_bolim(..., bolim_ilova=[...])`** — a group-less sibling of the `ilova=` mechanism, because Xarita has no sub-sections to key on; it renders the same indigo `.guruh-ilova` card at the top of the section, one per map (`bolim_ilova` is a **list**; `bolim.html` loops it).
    - The Word table's three content columns become **four on the site**: a *«Video»* column was inserted third and the tools column pushed to fourth. **The .docx itself is untouched** — this column exists only on the site. Both short labels ("Video", "Anjomlar") are the client's own shortening of the Word wording ("Vidyosi", "Kerakli ish anjomlari"), which crowded the header. The T/r column did *not* become a fifth column; the step number is a badge floated inside the first cell, otherwise the four content columns get squeezed.
    - The 13 numbered files in the source folder map straight onto the 13 rows, but only 9 of those are video — **1, 11 and 12 are stills**, so the video column has two cell shapes (`tur` = `video` / `rasm`). Anything iterating the column mechanically has to handle both.
      - **Row 13 is a later, separate addition**: the client's full 4-minute tutorial (`Qog'ozdan_qalam_stendi…mp4`, 640×360, 16:9 with no bars). It differs from the other nine in one way — it **keeps its audio**, since it is a narrated lesson rather than a silent loop; the tile still starts muted. Its poster is **not** a frame from the clip: `poster13.jpg` is the finished-object still rendered from `13.png` at 760px, because the client wants that picture on the tile. (A frame grab was tried in between and rejected — so re-encoding the video does not mean regenerating this poster.) At 16:9 in a 1.133 tile the `cover` crop is heavy (~36% of the width), so this is the row where the lightbox matters most.
    - **The source videos are HEVC (H.265) 1080p and must never be served as-is**: Chrome plays HEVC on Windows only if the OS extension is installed and Firefox not at all, so the pupil would get a black rectangle with no error. All nine are re-encoded to H.264 (`yuv420p`, CRF 26, `+faststart`) with **audio stripped at the file level** (`-an`) rather than relying on the `muted` attribute. They are also **pillarboxed** — every clip has symmetric black bars of a *different* width (371px … 186px per side, measured by scanning 5 frames per clip for the widest non-black extent; `cropdetect` reported nothing and is not to be trusted here), so each is cropped individually before scaling to 720px tall. 49 MB → 8.3 MB.
      - **All three media columns must match**: `--ql-media` on `.ql-jadval` sets one height for the picture, video and tools boxes (they were 150/202/116px and the row looked ragged — the client flagged it), and the three columns share one width. Change the variable, not the individual rules.
      - Tiles use **`object-fit: cover`**, so the media fills its box edge to edge — the client rejected the letterboxed look ("full qilish kerak"). The trade-off is real and was accepted knowingly: the crops leave aspect ratios from 1.09 to 1.43 against a ~1.25 box, so the narrowest clips lose ~6% top and bottom and the widest lose ~6% left and right, and the small source pictures (164–275px) are scaled up. The **lightbox is the escape hatch** — it uses `contain` and never upscales, so the true frame is always one click away. Do not "fix" the tiles back to `contain` without checking with the client.
      - Videos live in `media/xarita/qalamdon/`, not `static/`: WhiteNoise gzips `.mp4` (it is not in its skip list) and would commit a second copy into `staticfiles/`. `home.W003` in `home/apps.py` therefore checks the videos too — they pass through no build step, so a missing one is silent.
    - Every media cell opens in a **shared lightbox** (`#ql-modal`, one per page — 13 hidden `<video>` elements would otherwise sit in the DOM), filled from `data-katta`/`data-manba`/`data-sarlavha`/`data-izoh` on the clicked button. Video cells offer *both* routes deliberately: the tile itself plays in place, and a separate "Kattalashtirish" button below opens the big view. Closing empties the modal's media container, which is what stops playback and cancels the download. Durations live in `_DAVOMIYLIK` (hard-coded, not probed per request) and now show only in the lightbox title: a `VIDEO 0:08` corner badge was tried on the tile and the client asked for it off — the play button alone carries "this is a video", and the badge sat on top of the picture.
    - Rows alternate **`--sadaf` #E3F1F1 (palest turquoise) and `--qumoq` #FBF1DF (palest saffron)** — the fifth such pair on the site, after Metodika, Maqola, the Rasmli test and the Diktant; the tokens live in `qalamdon.css` because only this page uses them. They are the page's own header colours (`--firuza`, `--zafaron`) at their lightest, which is why they don't read as a foreign palette. The `tr:hover` tint was **removed**: it washed the row's colour out and broke the alternation. In card mode (≤860px) the `tr` rule deliberately sets no background, so the same `nth-child` tints carry through.
    - The step number and its text are a flex row inside a `.ql-qadam-ichi` **div**, not the `<th>` itself: `display: flex` on a `<th>` stops it being a `table-cell` and drops it out of the row. `float` on the badge was the previous approach and left the text ragged — first lines beside the badge, later ones under it.
    - The header block above the table is deliberately spare, at the client's request: heading and hint centred, hint reduced to one pill-shaped line, and the `<caption>` deleted because it repeated the page lede word for word. The four header cells step through the palette — `--siyoh` → `--siyoh-3` → `--firuza` → `--zafaron` — each with an icon over a centred label. Note `--zafaron` is light enough that its cell needs `color: var(--ink)`; white text on it fails contrast.
    - **Do not put a `position: sticky` header on this table.** `.ql-jadval-quti` needs `overflow-x: auto` (the table has `min-width: 940px`), and `overflow-x: auto` makes `overflow-y` compute to `auto` as well — the wrapper becomes the sticky scrollport instead of the page, so the header does not follow the navbar and instead offsets itself by `top` *over the first row*. It looked exactly like a z-index bug.
    - Poster frames are grabbed at 55% of each clip's duration (an early frame catches the intro animation) and downscaled to 640px — they are fetched on page load, unlike the other images which are `loading="lazy"`.
    - **A truncated image response is what a stale WhiteNoise index looks like here, and it is vicious.** The same startup-cached `Content-Length` that truncates a grown CSS file (see the static-URL section) also truncates *images*, and a half-delivered JPEG does not fail politely: the tile paints as a thin strip along the top of its box, and Chrome's renderer can die outright (`Abnormal renderer termination`, every screenshot attempt failing). Both symptoms invite wrong theories — a broken `object-fit` rule, then progressive-vs-baseline JPEG encoding — and an A/B that *seems* to convict the encoding is easy to build by accident, because the same file loads fine over `file://` and only breaks over `http://`. **The one-line check that settles it: compare `stat -c%s` on the file in `staticfiles/` against the `Content-Length` the server sends.** If they disagree, nothing about the file is wrong; restart the server.
      - Windows makes this trap easy to re-enter: two `runserver` processes can end up bound to the same port, so killing one PID leaves the *old* stale-index server answering while you believe you restarted it. `netstat -ano | grep ':PORT' | grep LISTENING` lists every one — kill them all before concluding anything from a page load.
    - **The page name lives in exactly one place per map: `SARLAVHA` in `home/qalamdon.py` / `home/xonqizi.py`.** It feeds the `<h1>` and the `/xarita` call-to-action card. Two things ride alongside it and must not be forgotten when it changes: **`QISQA`** (`«Organayzer»` / `«Xonqizi»`) is what the breadcrumb and the `<title>` use — the full sentence is 60+ characters, which wraps the breadcrumb onto two lines and truncates to «Origami usulidan foyd…» in a browser tab; and **`TAVSIF`** (the lede) must not repeat the heading — the old lede started with the very words that are now in the `<h1>`. The long heading also needs `.ql-head h1` / `.xq-head h1` to widen `max-width` past `platform.css`'s `20ch` (same specificity, later file), or it breaks to three lines and stretches the banner.
    - **The second map, «Applikatsiya "Xonqizi"»** (a paper-applique ladybug), is `/xarita/xonqizi` → `xonqizi_sahifa` → `templates/xonqizi.html` + `static/css/xonqizi.css` (`.xq-` prefix), data in `home/xonqizi.py`, from `malumotlar/texnologikxarita/xonqizi/`. Structure, markup and JS are a deliberate copy of the Qalamdon page — the two are siblings and must look the same; the CSS is duplicated rather than shared because Google-Fonts-style `@import` chaining is banned here and because every page on this site carries its own colour pair (this one is the **sixth**: `--marjon` #FCEAE7 / `--zangori` #E9EEF7, the palest anor and the palest siyoh — the ladybug's own red-and-dark). Four things differ from Qalamdon and are worth knowing:
      - **12 Word rows, 11 clips, and the offset is the client's own instruction**: row 1 (laying out tools) has **no video**; rows 2–12 take the clips in order, so row 2 plays clip 1. Row 1's video cell instead shows **row 12's step image** — the finished ladybug — again at the client's request, so a pupil sees the goal before starting. (A dashed "no video" placeholder was there first and was rejected.)
      - **The tiles use `contain`, not `cover`** — the opposite of Qalamdon, and again the client's call: they asked that every picture be fully visible without opening the lightbox, because `cover` was slicing the ladybug's antennae off. `.xq-quti` is a centring flex box and the `<img>` keeps `width/height: auto` with `max-*: 100%`, so pictures are never upscaled either. The video tiles are still `cover`; if that ever has to match, change `.xq-pleyer video`.
      - **The source folder is a trap.** Clips sit in directories `1/`…`11/` (directory number = display order) with confusing filenames (`11-1`, `12-1`, `2-1`, `51-1`, `10-2-1`…), and each directory *also* holds a stale, shifted copy from an earlier arrangement. The client split 6 long videos into 11, and the durations prove the split exactly — `1 (60.37s) = 11 + 12`, `5 (22.31s) = 51 + 52`, `6 (94.11s) = 61 + 62 + 10-1 + 10-2`. Take the **newest** file in each directory; `_LAVHA` in `home/xonqizi.py` records the mapping, so trust it over the filenames.
      - Videos were HEVC 1080p like Qalamdon's and are re-encoded the same way (H.264, `yuv420p`, 1280px, CRF 26, `-an`, `+faststart`; 227 MB → 16 MB) — but these are **clean 16:9 with no pillarbox bars** (verified: 0 px both sides), so unlike Qalamdon nothing is cropped before scaling.
      - Images come straight from the Word (`qadamNN.jpg` = step, `anjomNN.jpg` = tools, `posterNN.jpg` = frame at 55%). They are saved with **`subsampling=0`**: the object is red on white, and JPEG's usual 4:2:0 chroma subsampling mangles exactly those red edges.
      - `home.W004` in `home/apps.py` checks all 35 files, mirroring `W003`.
  - Two traps that bit during this work, both silent: (1) `.material.has-muqova` **must** declare explicit `grid-template-columns` — with `auto` in the first column, Chrome sizes it to the `alt` text before the image decodes and crushes the text column to zero width; and it needs re-declaring inside the 660px media query, since `.material` alone loses on specificity. (2) `{# … #}` is **single-line only** in Django — a multi-line one renders as visible page text.
- **Section 1 (`haqida.html`) is hand-written** — author bio, ta'lim/faoliyat timeline and publications are filled in from the client's public BuxDPI profile. The author photo is optional: drop a file at `media/muallif.jpg` and the view's existence check swaps the motif placeholder for the image.
  - The page then repeats the **same `.muallif` layout for the `#ilmiy-rahbar` band** (the client's dissertation supervisor, Saidova M. J., from her own BuxDPI employee page) — sticky `.muallif-card` + `.prose` column, so the two profiles read as one system. Only the extras are page-local CSS: `.rahbar-son` (the 14+/43/200+ counters), `.rahbar-ish` (the two dissertations) and `.rahbar-nishon`. Her photo follows the same optional-file rule at `media/rahbar.jpg` (`rahbar_foto` in the view). Nothing here is a `Material` row — like the author bio, it is deliberately hand-written. The page ends with a `#portfolio` band holding a PDF from `media/hujjatlar/`; its inline viewer sets the `<iframe>` `src` only on first click, so the file isn't fetched on page load — keep that if you add more documents.
  - Client PDFs are Canva exports, which store **photographs as PNG** and blow the file up ~7×. Recompress before serving: `doc.rewrite_images(dpi_threshold=170, dpi_target=150, quality=88)` in PyMuPDF took the portfolio from 57.7 MB to 7.9 MB with the lowest page PSNR at 41 dB (visually identical, QR codes still decode). Originals stay in `malumotlar/`.
- **SEO / social meta lives in `base.html`'s `<head>` (2026-09-09).** Canonical, robots, keywords, author, theme-color, the full Open Graph set and Twitter card. Every URL is built from `{{ request.scheme }}://{{ request.get_host }}` rather than a hard-coded domain, so aniko.uz and texnoedu.uz each advertise themselves and a new domain needs no template edit; under `DJANGO_HTTPS=1` the scheme comes out `https` through `SECURE_PROXY_SSL_HEADER`. **The gotcha:** Django forbids the same `{% block %}` twice in one template, so `og:title` cannot reuse `{% block title %}` — there are parallel `og_title`/`og_description`/`twitter_*` blocks, and a page that overrides its title must override those too (`bolim.html` and the nine named templates already do). `oquvchilarim.html`, `oquvchi_natija.html`, `login.html` and `register.html` carry `noindex`. The share image is `static/img/og.jpg` (1200×630), generated from `bosh-fon.jpg`: it is **fitted, not cropped** — the hero is 2.19:1 against OG's 1.905:1, and removing those 240px cuts one of the client's two hand-lettered taglines, so the bands top and bottom are filled with the image's own edge colours instead.
- **The site's name is «Inkorporatsion amaliy topshiriqlar»** — the word *amaliy* was added on the client's instruction (2026-09-09) after it had been missing site-wide. It lives in: `base.html` (`<title>`, description-adjacent meta, `og:site_name`/`og:title`/`og:image:alt`/`twitter:title`, keywords, navbar `.brand-name`, footer brand and copyright), `index.html`/`bolim.html` title+og+twitter blocks, `haqida.html` and `index.html` prose, `login.html`/`register.html` meta + `.auth-brand .name`, plus comments in `core/settings.py`, `platform.css`, `requirements.txt` and `DEPLOY.md`. Two rendered places carry it as *layout*, not just text: the hero pill `.bosh-tabletka` (whose `font-size` dropped from `7.8cqw` to `6.8cqw` because «amaliy topshiriqlar» is 1.6× the old string and would otherwise have matched the rainbow «Inkorporatsion» above it edge for edge) and the navbar brand, where *amaliy* went on the **green second line** rather than the blue first one — that keeps the brand block's width at the blue line's ~142px and so leaves the millimetre-measured navbar ladder untouched.
- Nav highlighting: each view passes `active` (e.g. `'maqola'`) and `base.html` compares it per link.

### Design system (`static/css/platform.css`)

The **logo** is the client's circular craft emblem, kept in `logo/` as the original JPG and published as `static/img/logo.png` (256px, white background flood-filled to alpha, PNG-8 ≈ 34 KB). It is the brand mark in the navbar and footer (`.brand-logo`) and the source of the favicons (`favicon.ico` 16/32/48, `favicon-32.png`, `apple-touch-icon.png` — all three linked from every template's `<head>`, including the standalone exercise pages, which do not extend `base.html`). Regenerating: Pillow is **not** in `env/` — run image scripts on system Python (`py -3.11`), same as PyMuPDF. `#palak` is still the decorative motif everywhere else; it is just no longer the logo.

**The file is two eras stacked** — read it bottom-up. The top half is the original 2026-07 "Suzani" system (indigo `--siyoh` chrome, pomegranate/saffron/turquoise, **Bitter** + **Karla**, photographic page heads); the blocks from "OQ NAVBAR" onward (**YORQIN USLUB — BUTUN SAYT**, **BO'LIM TUSLARI**) re-skin it to the client's bright mockup: white navbar, blue `#2E7DE0` primary, green `#43A047` action buttons, **Fredoka** (display) + **Nunito** (body) + **Caveat** (handwritten), pill buttons, pale sky-blue surfaces, and one tint per section via `<body class="sahifa-{{ active }}">` → `--tus`. The old blocks were kept because they still hold the *layout* and responsive rules; the late blocks only override colour, type and shape with equal-specificity selectors — so a fix to a colour goes in the late block, a fix to a layout in the early one. The `#palak` rosette remains the decorative motif (eyebrows, card corners, empty states).

Four rules worth knowing before you edit it:
- Link colour is scoped to `.prose a` / `.material-body a` **on purpose**. An earlier global `.pf a { color: … }` out-specified `.btn`, `.bolim-card` and `.brand` (all single-class selectors) and tinted every button and card title. Don't reintroduce it.
- Photographic backgrounds sit on `::before` at `z-index: -2` with a scrim on `::after` at `-1`, under `isolation: isolate`. Hero/`page-head` images live in `static/img/` (`hero-qogoz.jpg`, `sinf.jpg`, `qollar.jpg`).
- Global text scale is `html { font-size: 108% }` — a percentage, not px, so the browser's own font-size setting still applies. Everything else is in `rem`, so bump that one number rather than editing sizes one by one.
- **The navbar is one row and sized to the millimetre.** (The ladder below was measured with Karla; the bright re-skin switched `.nav a` to Nunito and re-checked that the bar stays 75px/one row from 1280 to 1920 — re-measure if the font changes again.) Seven long Uzbek labels + brand + auth barely fit, so the sizes come from *measuring*, not from eye. The binding case is a **logged-in** user with a long name — `.nav-user` + "Chiqish" add ~199px that the logged-out view never shows, so always check both. Three things move together: `.topbar .wrap`'s `max-width: 1340px` (wider than the 1200px content `--wrap` on purpose), the hamburger breakpoint, and `.nav a`'s font-size/padding (with `.nav { gap: 0 }`). Enlarge any of them and the breakpoint has to rise too, or "Chiqish" gets clipped by `overflow-x: hidden`.
  - **The size is a four-step ladder, not one number.** `0.94rem` is only the *base* step, chosen for the 1366px laptop; on a wide monitor that left the labels looking tiny against empty gutters, so `min-width` queries scale the whole navbar up. Each step raises `.topbar .wrap`'s `max-width` **together with** the font sizes — bump the font alone and the links stay crushed into 1340px and wrap to a second row.

    | Viewport | `.nav a` | `.wrap` max-width | measured content | needs |
    |---|---|---|---|---|
    | ≤1279px | hamburger | — | — | — |
    | 1280–1365px | 0.88rem / 7px | 1340px | — | — |
    | 1366–1449px | 0.94rem / 7px | 1340px | 1325px | 1340px |
    | 1450–1559px | 1.00rem / 9px | 1420px | 1411px | 1426px |
    | 1560–1659px | 1.06rem / 11px | 1510px | 1502px | 1517px |
    | ≥1660px | 1.12rem / 13px | 1600px | 1594px | 1609px |

    "Measured content" is the `.wrap` box width **including its own 56px padding**; "needs" adds ~15px for the scrollbar. Every step keeps 25–50px of slack. Note the older figures in this file (1279px for 0.94rem, "+48px") were *ink* spans from pixel-scanning, which exclude that padding — don't mix the two conventions or you will double-count.
  - **Measuring without a browser session or a running page:** give a throwaway page `.topbar .wrap { max-width: none; width: max-content }`, stack one navbar copy per candidate size, and load it over `file://` in headless Chrome — no server, no `staticfiles/` restart dance. Two ways to read the number back, both used here:
    - *Preferred:* let page JS `await document.fonts.ready`, then read `getBoundingClientRect().width` and print it into the DOM. Exact, sub-pixel, and it can assert `document.fonts.check('600 1rem Karla')` — **if the fonts fell back, every width is wrong**, so check this before trusting a run.
    - *Pixel scan:* screenshot, then in Pillow (`py -3.11`) find the left/right-most pixel differing from `--siyoh` (#14203A). Skip the band's top/bottom few rows or the 1px `border-bottom` reads as full-width content. Good for verifying the *shipped* breakpoints: render at each width and check the dark band stays exactly **74px tall** — a second row is the failure this whole section exists to prevent.
    - `--dump-dom` exits 21 with empty output if `--user-data-dir` is a relative path; pass an absolute Windows path. `--screenshot` works either way.
    - **`--virtual-time-budget` does not advance CSS transitions.** A `setTimeout` fires instantly in virtual time, but a `transition`ed property is still at its *starting* value when you read it — so `getComputedStyle` reports the old colour and the change looks broken. This cost real time while checking the diktant's selected-number style: `background-color`/`color` are transitioned and read as unchanged, while `opacity` (not in the transition list) read correctly, which made it look like one CSS rule out of two had failed to apply. Add **`--force-prefers-reduced-motion`** when asserting on transitioned properties, or screenshot without a virtual-time budget.
    - Injecting a test script into a page fetched with `curl`: write the `<script>` to its own file with a `<<'JSEOF'` heredoc and splice it in with Python. Embedding the same JS *inside* a `python -c "…"` string mangles the escaping and the script silently never runs — you get an empty result that looks like a page bug.
  - Beware when screenshotting: a *fresh* `--user-data-dir` has no font cache — if the network is down, `fonts.googleapis.com` stalls and `load` never fires, so anything keyed to `window.onload` (including page JS you're trying to test) silently never runs. The user's own Chrome usually holds a lock on the default profile, so a throwaway profile is the only option.
  - Renaming a nav label re-opens this. "Instruksion xarita" → "Instruksion texnologik xarita" alone cost +88px, which is what forced `.nav a` down from `0.92rem`/`9px 9px`. To measure without guessing: drop a throwaway page into `staticfiles/` with the logged-in navbar markup wrapped in a `width: 1280px` box, and screenshot it with headless Chrome (`--headless=new --window-size=1500,150 --screenshot=…`). Screenshotting at `--window-size=1280` directly is unreliable — the headless viewport comes out just under 1280 and the hamburger fires, which looks like an overflow bug but isn't.

### Administrator sahifasi — «O'quvchilarim» (2026-09-05)

`/oquvchilarim` lists **every** user with their `Result` rows, and `/oquvchilarim/<id>` is one person's full history. Both are gated by `admin_required` in `home/views.py` — **`is_staff`, not the `talaba` role**: "O'qituvchi" is a self-service role at registration, and a teacher must not see 1100 other people's personal results. Anonymous → `/login?next=…`; signed in but not staff → `/` (silent, same as `talaba_required`).

- **The navbar could not take an eighth link.** It is measured to the millimetre (see the `.topbar .wrap` ladder above), so instead the staff user's **name became a menu** (`.nav-hisob` in `base.html` + platform.css): O'quvchilarim / Boshqaruv paneli / Chiqish. «Chiqish» moved *into* it, so the block got **15–38px narrower** than the old "name + Chiqish" pair at every step (measured figures are in the CSS comment). A non-staff user's navbar is byte-for-byte unchanged. The menu opens on **click only** (JS in `base.html`), not hover like «Topshiriq» — a stray mouse must not reveal a logout button.
  - `.hisob-ism`'s `max-width` lives **next to `.nav-user` near the top of platform.css**, not in the late bright block. A media query adds no specificity, so a `max-width` written at the end of the file silently overrides all four responsive steps — that is exactly what happened first, and the name stayed 84px wide on a 1920px screen.
- Section tint: `--t8` (#1E7A6B zumrad) + `.sahifa-oquvchilarim`, added to platform.css the way that file's own comment prescribes. There is no `--bolim-rasm` photo for it, so the `.page-head` falls back to its gradient — that is deliberate, not a missing file.
- Page CSS is `static/css/oquvchilar.css` (`.oq-` prefix), loaded per page via `{% block head %}` so `platform.css?v=` only had to be bumped for the navbar change.
- **Aggregates.** Per-user `urinish` / `otgan` / `ortacha` / `oxirgi` are one annotated queryset; the page's 20 users then get their `Result` rows in **one** extra query, grouped in Python (no N+1). Two traps, both silent:
  - Filtering by exercise uses `qs.filter(id__in=Result.objects.filter(mashq=…).values('user_id'))`, **never** `qs.filter(result__mashq=…)` — the latter reuses the annotation's JOIN and would recompute every average over that one exercise.
  - `_foiz_ifodasi()`'s `Case` defaults to `Value(None)`, not `0.0`. With `0.0` the LEFT JOIN's single empty row made `Avg` return 0, so a user who had never taken anything showed «0%» instead of «—».
- CSV export (`/oquvchilarim/eksport?tur=royxat|natija`) reuses the same filters through `_saralangan_royxat()`, writes a **UTF-8 BOM** and uses `;` as the delimiter — without both, Excel mangles the Uzbek text and puts every row in one column.
- Mobile (≤980px) turns the table into cards with `td::before { content: attr(data-yorliq) }`. The label sits **above** the value, not beside it: a two-column `display: grid` on the `<td>` was tried first and broke the «Natijalari» cell, where each result chip became its own grid item and stretched to full width on its own row.
- `TIME_ZONE` moved from `'UTC'` to `'Asia/Tashkent'` in the same change — this page is the first one that shows clock times, and they were five hours behind. Storage is unaffected (`USE_TZ = True`).

### Auth pages

`login.html` and `register.html` are standalone (no `base.html`) but belong to the **platform** layer: they load `platform.css` + `static/css/auth.css` and use its tokens. They used to load the kids' `style.css`, which is why they were violet/pink — if either page ever looks off-brand again, check which stylesheet it links. `auth.css` is shared by both; the background uses the same `::before` image / `::after` scrim trick as `.page-head`.

### Exercise pages

The 10 exercises are unchanged and keep their own look: they load `static/css/style.css` (the vivid kids' theme with `.t-*` classes) and do **not** extend `base.html`. Their `.back-link` points at `/multimediya`, not `/`. The `talaba_required` decorator also bounces to `/multimediya`.

**Krossvordlar — `static/css/krossvord.css` (2026-09-05).** O'nta krossvord sahifasi (`/m6c`…`/m10c`, `/o6c`…`/o10c`) har biri o'zining ~90 qatorlik, deyarli bir xil inline `<style>` blokini olib yurardi. Mijoz «juda ham kichik» deb shikoyat qildi: katak o'lchami `38px` qilib qo'lda yozilgani uchun 1920px li monitorda plitaning yarmi bo'sh turar, to'r esa o'sha 630px bo'lib qolardi. Endi hammasi bitta faylda va sahifa faqat ikki raqam beradi: `.crossword-grid { --kv-ustun: 15; --kv-qator: 16; }`.
- **Katak o'lchamini endi joy belgilaydi**: ustunlar `minmax(0, 1fr)`, katak `aspect-ratio: 1`, to'rning `max-width` i `ustun × 60px`. Harf, digraf va raqam shriftlari `container-type: inline-size` + `cqi` bilan katak bilan birga o'sadi (`calc(42cqi / var(--kv-ustun))` ≈ katakning 42% i), `max(...)` esa pastki chegara beradi. O'lchangan: 1280px da katak **38 → 54px**, izoh shrifti **15.9 → 17.6px**.
- **Joylashuv chegarasi 1200px dan 1700px ga ko'tarildi.** Uch ustunli joylashuvda (savollar | to'r | savollar) savollar ikki chetdan ~600px olib qo'yadi, ya'ni 1280px li ekranda to'rga atigi 550px qolardi — eski qat'iy 630px dan ham tor. 1700px dan tor ekranda to'r tepaga chiqadi, savollar ostida ikki ustunda turadi.
- **Aniqlik tuzog'i:** krossvordning rangi va shakli `style.css` da `.quiz-card …` selektorlarida (0,2,0). Yangi fayl keyin ulansa ham `.crossword-grid` (0,1,0) ular bilan bahslasha olmaydi — birinchi urinishda o'lchamlar ishladi, fon/chegara/sarlavha esa yo'q. Ko'rinishga tegadigan har bir qoida `.quiz-card` bilan boshlanishi shart.
- **Inline blokni olib tashlaganda nima yo'qolishini tekshiring.** `.num-badge` ning `position`/`border-radius`/`display:flex` i faqat o'sha blokda bor edi (`style.css` unga faqat RANG beradi), natijada raqam katak tepasida ko'k to'g'ri to'rtburchak bo'lib cho'zilib qoldi. Aksincha, fokus halqasi, to'g'ri/xato ranglari, kalit va oldindan to'ldirilgan katak `style.css` da bor — ular `krossvord.css` da ataylab TAKRORLANMAYDI.

**«Oq fonda oq matn» tuzog'i — `mashq3.css` (2026-09-05).** Mashq 3 («Topishmoq Arena») panellari ilgari rangli gradientda edi va matni oq, «shisha» uslubida (`rgba(255,255,255,.2)` + `backdrop-filter`). Yorqin uslubga o'tishda panellar oq plitaga aylantirilgan, lekin ichkaridagi ikkita element e'tibordan chetda qolgan — **topishmoq matni butunlay ko'rinmay qolgan** (mijoz aynan shuni ko'rsatdi), yakun ekranidagi «To'g'ri/Noto'g'ri» raqamlari ham. Uch xulosa:
- Tuzatuvchi blok `.m3-summary-value` / `.m3-summary-label` ni nishonga olgan, HTML da esa `.m3-stat-val` / `.m3-stat-label` — ya'ni qoida hech qachon hech nimaga tegmagan. **Yangi qoida yozganda sinf nomi shablonda borligini tekshiring**, aks holda xato sokin qoladi.
- Fayldagi barcha `color: #fff` + `rgba(255,255,255,…)` juftliklarini bir marta ko'rib chiqing: bu naqsh faqat `mashq3.css` da bor (qolgan mashqlarda oq matn har doim to'q fonda).
- Yorqin uslub `--siyoh` ni to'q indigodan **ko'k `#2E7DE0`** ga o'zgartirgan, shuning uchun zafaron tugmalardagi `color: var(--siyoh)` ko'k-to'q sariq juftligiga aylanib qolgan — kontrasti 1.9:1, o'qilmaydi. `mashq3.css` da u `var(--ink)` ga (5.5:1) o'tkazildi; **`style.css` dagi `.check-btn` va boshqa mashqlarda hali eski holida** — mijoz so'rasa, o'sha yerda ham bir qatorlik tuzatish.

**Every page is a function view in `home/views.py` that renders one template.** There is no client-side framework and no JS build step. To add or change a page you touch three places in lockstep:
1. Template in `templates/` (or `templates/oquvchi/`)
2. A `render(...)` view in `home/views.py`
3. A `path(...)` route in `core/urls.py`

Running `manage.py check` after route/view edits catches most mistakes.

**The scene layer (2026-09-05).** The client said the pages looked "juda ochiq" (far too empty) next to a colleague's fully-illustrated slides, and asked for images first, colour second. All 37 exercise pages are separate HTML files but share `style.css` and carry a `t-*` theme class on `<body>`, so the whole decoration is driven from one block at the end of that file ("SAHNA"), with no per-page edits:
  - `body` background = a full-bleed illustrated scene, `fixed`/`cover`. Three scenes live in `static/img/sahna/` (`bogh`, `tong`, `orzu`) and are assigned per theme class. They are **hand-generated SVG** (the generator is in the session scratchpad, not the repo): sky, sun, clouds, layered hills with aerial perspective, trees, bushes, flowers, grass tufts, balloons, a kite. ~30 KB each, license-clean, sharp at any size.
  - `body::before` = a white veil (so text stays readable) **plus four craft props** as extra background layers, positioned in the empty side gutters and slowly drifting via a `background-position` keyframe. Props are 17 flat SVGs in `static/img/sahna/anjom/` (scissors, glue, pencil, paper, palette, yarn, origami crane, clay pot, ruler, brush, star, cube, chef hat, toolbox, felt, quilling, plasticine). Each theme picks its own four.
  - `body::after` = the client's own two-children illustration as a corner mascot, `z-index: -1` so a wide card (the crossword) passes in front of it instead of colliding.
  - **Trap:** the original `body::before` rule near the top of `style.css` sets `opacity: .5` for the old paper-fibre texture. Any later reuse of that pseudo-element inherits it — the props rendered at half strength and the blue scissors came out grey. The scene block resets `opacity: 1` explicitly.
  - `media/bolalar.png` is RGBA but its alpha channel is entirely 255, i.e. the white background is painted in. The cut-out (`static/img/sahna/bolalar.png`, PNG-8, 60 KB) was made by flood-filling from the border, then two dilation passes to eat the light halo and the ground shadow. Use that file for anything placed over the scene.
  - `templates/multimediya.html` cards now carry a coloured header band with the matching topic prop instead of an emoji. Real photos were deliberately **not** used there: the available ones come from three different sources (scans, clipart, photos) and looked scattered side by side.
  - `rasmli-test.css` and `diktant.css` each append their own copy of the scene (they extend `base.html`, so they load `platform.css`, not `style.css`, and `body::before`/`::after` are free there). `platform.css` gives the teacher-facing pages a lighter treatment: side props only, at `opacity: .55`, `:not(.auth-page)` so the auth pages keep their own clouds and sun.

**Section photos (2026-09-05).** After the scene layer the client still said the site read as plain and asked for real downloaded photographs on nearly every section. Each of the seven sections now has a wide photo band in its `.page-head`, from **Pixabay** (Pixabay Content License: free for commercial use, no attribution required). Files are `static/img/bolim/<section>.jpg`, 1500×560, JPEG q80, 50–160 KB; the source URLs and the crop code are in the session scratchpad (`bolim_rasm.py`), and the CSS variable is `--bolim-rasm`, set per `sahifa-*` class.
  - The photo sits between a **left-to-right white scrim** (opaque where the heading and lede are, transparent on the right so the picture shows) and the pale fallback gradient, all inside the existing wave `mask-image`. Below 900px the scrim flips to top-to-bottom, because the text then spans the full width.
  - The client vets these picture by picture: the first Multimediya banner (a sewing workshop) and the first Xarita banner (manicured hands folding paper) were both rejected and swapped for a child at a laptop and a set of colourful origami animals. Expect to swap on request; only `bolim_rasm.py` and one `collectstatic` are involved.
  - **Do not guess Pixabay URLs** — invented `cdn.pixabay.com` paths return 403. Search `https://pixabay.com/images/search/<query>/` (that page loads from here; the Pexels and Unsplash search pages return 403/401) and pull the real `cdn.pixabay.com/photo/...` links out of the HTML, then swap `_640` for `_1280`.
  - **Trap that produced a white rectangle on phones:** a leftover rule in the 660px media query still set `background-size: 220px 132px, 0 0, 100% 100%` from when the layers were two clouds and a gradient. With the new layer order that sized the white scrim to a 220×132 box and the photo to nothing. When you change what a multi-layer `background-image` contains, grep every media query that sets `background-size`/`background-position` for that selector.

**The five-colour card cycle (2026-09-05).** The client's last requirement after the imagery was that no card stay white. One rhythm now runs across the whole site: green, blue, saffron, pink, violet, repeating with `:nth-child(5n+k)`. It is applied to material cards in `bolim.html` (each also drives its left edge, file badge and "Ko'rish" button through a `--kart` variable), the seven landing cards, test question cards and their A/B/C/D letter badges, crossword clues, the checkbox rows in exercises 1-5, and the question cards in the Rasmli test and Diktant (which previously alternated only two tints). Two invariants survive it:
  - **State colour always wins.** Green means correct, red wrong, blue selected. Those selectors carry two classes (`.option-btn.correct`), so they out-specify the decorative cycle; where a single-class rule could have lost, the state rule is restated after the cycle.
  - **Picture boxes stay pure white** on the Rasmli test and Diktant. Their images were flattened onto white, so any tint behind one draws a white rectangle around the picture.

**Exercise structure:**
- **Exercises 1–5** — interactive games. `@login_required` but **not** role-gated: any signed-in user sees them. One template each (`mashq1.html`…`mashq5.html`) plus a matching `static/js/mashqN.js` / `static/css/mashqN.css`.
- **Exercises 6–10** — three stages each: **video → test → crossword**, as templates `mashqNa.html` (video), `mashqNb.html` (test), `mashqNc.html` (crossword). The quiz and crossword logic live **inline in a `<script>` inside each HTML file** — not in external JS. (`static/js/mashqNb.js` files exist but the canonical logic is inline.)
  - **Exercise 9 breaks the a/b/c pattern**: it has a *second* video page, `m9a2` / `o9a2` (`mashq9a2.html`, `oquvchi/m9a2.html`), routed in all three URL schemes. Anything that iterates the exercises mechanically will miss it.

**Roles & gating** (`home/models.py` → `Profile`, roles `oquvchi` / `talaba`):
- `talaba` (or `is_staff`) sees the teacher pages at `/mNa`, `/mNb`, `/mNc`, protected by the `@talaba_required` decorator in `home/views.py`.
- `oquvchi` sees pupil pages at `/oNa`, `/oNb`, `/oNc` rendering `templates/oquvchi/mNx.html` — `@login_required`, but **not** role-gated. In short: *every* exercise page requires login; only `/mNx` additionally requires the role.
- Gating failures go two different ways, which matters when debugging a redirect: no session → `LOGIN_URL = '/login'` with `?next=`; signed in but wrong role → straight to `/multimediya`, no message. `login_view`/`register_view` return to `next` through `_safe_next()`, which validates the host (open-redirect guard) — keep that if you touch the auth flow.
- `templates/multimediya.html` picks the link per card via the `show_advanced` context flag (`= is_talaba(user)`): `{% if show_advanced %}/m6a{% else %}/o6a{% endif %}`.
- Pupil versions of exercises 6–10 are all complete (video → test → crossword for each). See `OQUVCHI_QOLLANMA.md` for the full step-by-step of adding/extending a pupil exercise (including crossword grid data format and digraph cells like `CH`/`SH`/`G'`/`O'`) — note it still describes exercise 9 as video-only, which is now out of date.

**Results:** completing a test POSTs to `/api/save-result` (`save_result` view, login-required). It writes a `Result` row keyed by `mashq` (e.g. `'m8b'`); a result counts as passed only at 100% (`Result.save`). Any new test key must be added to `Result.MASHQ_CHOICES` in `home/models.py` or the save fails.

### Ish stoli ilovasi — `.exe` (2026-09-09)

`ilova/` holds a **Windows desktop wrapper** for the live site: `ilova.py` (the launcher), `qur.py` (the PyInstaller build script), `belgi.ico`, `manzil.txt` and a client-facing `OQING.txt`. Output lands in `ilova/dist/` (gitignored, ~13 MB single file). Build it with `ilova/qurish-env/Scripts/python.exe ilova/qur.py`; recreate that venv with `py -3.11 -m venv ilova/qurish-env` + `pip install pywebview pyinstaller` — **not** in `env/`, which stays the Django runtime venv with nothing but the pinned requirements.

- **The renderer is Edge WebView2 (`gui='edgechromium'`), i.e. Chromium** — verified in-app as `Chrome/152`, `canPlayType('video/mp4; codecs=avc1…')` → `probably`. That is what makes the crosswords, the Rasmli test timer, the diktant and the H.264 tutorial clips work. The backend is **forced**, not auto-detected: pywebview's fallback is the old MSHTML (IE11) engine, which would render the site as garbage rather than fail loudly. When WebView2 is genuinely absent the launcher catches it and shows a `MessageBoxW` with the Microsoft download link instead of a traceback.
- **`private_mode=False` + a `storage_path` under `%LOCALAPPDATA%\InkorporatsionTopshiriqlar`** — without both, WebView2 runs incognito and the pupil is logged out every launch. This site is login-gated, so that is the difference between usable and useless.
- **The URL is a *list*, probed at startup, first reachable one wins**, and `manzil.txt` next to the .exe overrides the built-in list without a rebuild. This exists because the client's intended domain **`inkorporatsion-amaliy-topshiriqlar.uz` did not resolve when the app was built** (NXDOMAIN, both spellings) — so the fallbacks `texnoedu.uz` / `aniko.uz` are what actually open today, and the exe starts working on the new domain the moment DNS points at the server, with no new build. The probe treats an `HTTPError` as success: a 404 still proves the server is there.
- The window opens **immediately** on a local spinner page and the probe runs on a worker thread — a dead domain costs `KUTISH` (6 s) per entry, and a window that appears only after that reads as a hung app.
- PyInstaller args worth keeping: `--collect-all webview` and `--collect-all clr_loader` plus `--hidden-import pythonnet` (the .NET bridge to the WebView2 assemblies drops out of `--onefile` builds otherwise), and `--windowed` so no console flashes behind the window.

## Conventions & gotchas

- **Framing media needs `xframe_options_sameorigin`.** `XFrameOptionsMiddleware` stamps `X-Frame-Options: DENY` on *every* response, including `serve()`ing a PDF from `media/`. An `<iframe>` pointing at it then fails with Chrome's misleading "127.0.0.1 refused to connect" — which looks like a dead server, not a header. The media route in `core/urls.py` is wrapped in `xframe_options_sameorigin` for exactly this; everything else stays `DENY`.
- **Google Fonts must never be loaded with CSS `@import`.** `platform.css` used to start with one; an `@import` is a render-blocking stylesheet, so when `fonts.googleapis.com` is slow or blocked (school networks, offline dev) the page's inline `<script>` blocks never run — the Rasmli test froze with unclickable tiles and a dead "Tekshirish" button, which looks exactly like broken JS. The fonts now load from each `<head>` via `<link media="print" onload="this.media='all'">` (+`preconnect`, +a `<noscript>` fallback) in `base.html`, `login.html`, `register.html`. Same rule applies if `style.css` is ever touched — it still has an `@import` for the kids' fonts.
- **Static/media URLs are hardcoded absolute paths** (`/static/css/style.css`, `/media/Video1.mp4`) — the templates deliberately do **not** use `{% static %}`. Because of this WhiteNoise is configured to compress but not hash static files (`CompressedStaticFilesStorage`, see `core/settings.py`). Media is served at runtime via a `serve()` route at the bottom of `core/urls.py`.
  - **Consequence: a CSS edit can be invisible in the browser, and it does not look like a caching problem.** No hashing means the URL never changes, so a browser that has the old stylesheet keeps using it — the *new HTML* arrives while the *old CSS* styles it. The failure mode is a half-rendered page, not an obviously missing stylesheet: when the start modal was added to the Rasmli test, its markup appeared as unstyled text spilling over the header, which reads as broken HTML. Before debugging CSS that "doesn't apply", confirm the browser actually has the new file.
  - The fix in use is the same `?v=N` cache-buster the test images already use: `platform.css`, `auth.css` and `rasmli-test.css` are linked as `…css?v=2` from `base.html`, `login.html`, `register.html` and `rasmli_test.html`. **Bump that number in the same commit as the stylesheet edit** — WhiteNoise ignores the query string when finding the file, so the only thing it changes is the cache key.
  - Three separate caches sit between an edit and the screen, and they fail differently: the **template** cache (needs a server restart, `DEBUG=False`), **WhiteNoise's** startup index (needs a restart for *new* files), and the **browser** (needs `?v=` or a hard refresh). A change that seems to have had no effect is almost always one of these, not the edit.
- **Per-exercise theming:** design tokens are CSS custom properties in `:root` and theme classes `.t-orange`, `.t-blue`, `.t-rose`, `.t-indigo`, `.t-coral`, `.t-emerald`, etc. in `static/css/style.css`. Each exercise page sets `<body class="t-...">` to recolor. Exercise→theme map: 6=`t-blue`, 7=`t-rose`, 8=`t-indigo`, 9=`t-coral`, 10=`t-emerald`. Design direction is a vivid, premium, full-color kids' aesthetic — avoid plain white.
- **URL routing** in `core/urls.py` keeps three parallel schemes for each exercise: short (`/m6a`), legacy (`/mashq6a.html`), and pupil (`/o6a`). Keep them consistent when adding routes.
- **`malumotlar/`** holds the client's original source files (Word docs, uncompressed Canva PDFs) that the served material is derived from — e.g. `malumotlar/topshiriqlar/Rasmli test.docx`. It is **gitignored**, so it exists only on this machine: don't assume a future checkout can regenerate anything from it. `logo/` likewise keeps the original emblem JPG. Neither is part of the running app. (An older `namuna/` directory is gone.)
- **`staticfiles/` and `media/` are committed to git; `db.sqlite3` is NOT (since 2026-09-05).** Consequences: `collectstatic` output must be committed alongside the `static/` edit that caused it or production serves the old asset; every uploaded `Material` file shows up as a repo diff. The database was untracked at the client's request (it holds password hashes and personal data) — `git rm --cached` plus `.gitignore`, the file itself stays. Two things follow: (1) **the first `git pull` on the server that brings this change will try to delete the server's `db.sqlite3`** — copy it aside before pulling and put it back after; (2) DB content (admin edits, new users) no longer travels via git, it has to be copied by hand (`scp`), and the old committed copies are still in git history (purging needs `git filter-repo` + a force push, not done). Backups live in `malumotlar/db_zaxira_*.sqlite3`.
  - The DB holds **1101 generated demo users** (ids > 22; 2026-09-05) with realistic Uzbek names, mixed roles (~85% o'quvchi), registration dates spread over 2026-03..09 and ~3300 plausible `Result` rows — made to look like organic sign-ups at the client's request. The login/password list is `malumotlar/foydalanuvchilar_1101.xlsx` (gitignored); the generator is not in the repo. Real accounts are ids 1–22.
- **Settings are env-driven (2026-09-09).** `core/settings.py` reads an optional `.env` at the repo root through a ~12-line parser (no python-dotenv), and every knob falls back to a dev-safe default — so the file needs **no editing on the server**, and `git pull` never clobbers a server's configuration. Template: `.env.namuna`. Full deployment guide: **`DEPLOY.md`**. Four things follow:
  - `SECRET_KEY` is **no longer committed**. Order: `DJANGO_SECRET_KEY` env var → a `.secret_key` file the app generates on first run (gitignored, 64 chars) → an in-process random key if the filesystem is read-only. Deleting `.secret_key` logs every user out (sessions are signed with it); passwords are unaffected.
  - **The HTTPS hardening is opt-in via `DJANGO_HTTPS=1`, and that default matters.** SSL redirect, HSTS, `Secure` cookies and `SECURE_PROXY_SSL_HEADER` are all off unless the flag is set. It cannot default to on: this project runs `DEBUG=False` *in development too*, so an always-on `SECURE_SSL_REDIRECT` would bounce `http://127.0.0.1:8000` to `https://` and make local `runserver` unusable. `SECURE_PROXY_SSL_HEADER` is likewise only set behind the flag — trusting `X-Forwarded-Proto` on a directly-exposed server lets a client forge it.
  - `ALLOWED_HOSTS` keeps the six production domains as its default (`inkorporatsion-amayliy-topshiriqlar.uz` added 2026-09-10 — note the registered spelling is **amayliy**, not *amaliy*); `CSRF_TRUSTED_ORIGINS` is now **derived** from it (https + http per host, minus localhost) instead of being a hand-maintained 20-item list. Adding a domain means one `.env` line, not a code edit.
  - **The production server's `.env` is kept locally as `.env.server`** (gitignored; copied to the server as `.env`). Written 2026-09-11 from live checks of the server: Ubuntu + nginx 1.28 at 213.230.120.138, Let's Encrypt cert for the **apex only** (no `www`), nginx already doing http→https. Hence `DJANGO_SSL_REDIRECT=0` (no redirect-loop risk if nginx drops `X-Forwarded-Proto`), `DJANGO_HSTS_SUBDOMAINS=0` (www has no cert), HSTS 3600 to start, no secret key (auto-generated on the server). It sets **`DJANGO_ALLOWED_HOSTS` explicitly, which replaces the `settings.py` list on the server** — a domain added only in code will not take effect there. Never drop it in as `.env` locally: Secure cookies break login over `http://127.0.0.1`. The parser has no inline comments — `KEY=1  # note` reads as the string `"1  # note"`.
  - Errors go to `logs/xato.log` (RotatingFileHandler, 2 MB × 5). With `DEBUG=False` a 500 is otherwise silent — check that file first.
- **The database backs itself up.** `home/zaxira.py` + `manage.py zaxira`; `home/apps.py` calls `kerak_bolsa_zaxira()` from `ready()` and takes a copy into `zaxira/` (gitignored, last 14 kept) when the newest one is over 24 h old. Three details that are deliberate: it uses SQLite's own `.backup` API, **not `cp`** — under WAL the newest writes may still live in `db.sqlite3-wal` and a file copy would silently drop them; it fires only when actually serving (`sys.argv[1] == 'runserver'`, or waitress), guarded by `RUN_MAIN` so the autoreloader's parent process doesn't double it, because otherwise every `migrate`/`shell`/`check` would spam the folder; and it swallows every exception — a failed backup must never take the site down.
- **Login is rate-limited** (`home.xavfsizlik.KirishCheklovi`, last in `MIDDLEWARE`). Django throttles nothing on its own and the site has 1100+ accounts plus an open `/admin/`. It counts POSTs to `/admin/login/` (10 per 10 min → 15 min block) and `/login` (30 per 10 min → 10 min block), telling success from failure by the status code — a successful login always redirects (3xx), a failed one re-renders (200) — and clears the counter on success. `X-Forwarded-For` is trusted **only** when `SECURE_PROXY_SSL_HEADER` is set, i.e. behind the HTTPS flag; trusting it on a directly-exposed server would let an attacker rotate the header and bypass the limit entirely. The `/login` threshold is deliberately high because a whole school can share one NAT address.
- **SQLite runs in WAL mode** (`init_command` pragmas + `transaction_mode: 'IMMEDIATE'` in `DATABASES`, Django 5.1+ only). Readers no longer block on the writer, and a concurrent write waits 5 s instead of raising `database is locked`. Side effect: `db.sqlite3-wal`/`-shm` files appear next to the DB (already gitignored), and a live backup needs `sqlite3 .backup`, not `cp`.
- `requirements.txt` covers only the runtime (Django, whitenoise, asgiref, sqlparse, tzdata), is **UTF-8 and version-pinned** (it used to be UTF-16 and unpinned — a fresh install would have pulled whatever Django was current). `requirements-server.txt` adds `waitress` for serving in production. Pillow and PyMuPDF are deliberately absent: the image/PDF scripts run on system Python (`py -3.11`), never in `env/`.
